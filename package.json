{
  "name": "pwa-test",
  "version": "1.0.0",
  "description": "pwa-test",
  "scripts": {
    "start": "npx serve src",
    "generateAssets": "npx pwa-asset-generator src/logo.png src/icons --background orange --scape false --favicon true --manifest ./src/manifest.json --index ./src/index.html"
  },
  "license": "MIT"
}
